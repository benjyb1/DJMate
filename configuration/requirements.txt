# DJMate Backend API — production dependencies
# Keep lean for Render free tier (512MB RAM)
# For offline scripts (torch, librosa, umap), see requirements-scripts.txt

# Web framework
fastapi==0.129.0
uvicorn[standard]==0.40.0
python-dotenv==1.2.1
python-multipart==0.0.22
starlette==0.52.1

# Database
supabase==2.28.0
asyncpg==0.31.0
redis==7.1.1

# LLM providers
openai==2.20.0
anthropic>=0.40.0

# Numeric / ML (cosine similarity in online_learner)
numpy==2.4.2
scikit-learn==1.8.0

# HTTP client (used by llm_interpreter, supabase)
httpx==0.28.1

# XML parsing (rekordbox)
defusedxml==0.7.1

# Audio metadata (ingest)
mutagen==1.47.0
